# 投稿前内部大修分析方案

2026-08-31。依据本项目三份模拟评审及汇总，不是期刊决定或预注册方案。此前诊断结果已经可见；以下规则在本轮正式补分析运行前固定，不能称为结果盲法设计。

## 冻结与范围

保留 464 基因成员、GSE42783 参考方向、35 候选及 A18/B15/C2 等级。对既有结果只新增有版本的解释与证据列，不回写公开 1.0.0。原始实验数据不变。新增输出放在 `results/hr_revision_20260831/`。忽略并保留其他任务的 `results/v4/phase3_public_baselines_20260831/` 及 `.gitignore` 修改。

## 分析规则

1. 对照与消融。共同源基因按 GSE42783 绝对效应排序，保留 source-top-500、source-top-464、ranks-501-1000、后者经过 GSE108415 同方向确认的集合及冻结签名。所有集合的源定义不依据外部结果改变。给出方向率、效应秩相关、可测分母和与签名的差值，不把不同分母的点估计当作显著性检验。
2. 重复层面稳健性。分别在每个端点对已归一化生物学样本列有放回重抽样，整列一起抽以保留基因相关性。枚举所有有序抽样组合得到精确经验 bootstrap 分布；报告 2.5/97.5 百分位，不称为全流程或新研究泛化置信区间。源参考方向和基因集合固定。对照差值使用同一次重抽样，另穷举每端点各删一个样本的敏感性范围。样本量小的离散区间明确解释，测序 lane 已折叠，不作为独立重复。
3. 研究范围。上述对比覆盖四项成功转移和 GSE285925 两个边界层。补报 GSE183836 healthy 0 对 3 的既有健康子集，标注曾进入方法开发接口，只是开发来源复用敏感性分析，不增加外部研究计数。
4. 候选表。导出全部 35 基因逐研究效应、可测性、参考同向性、条件 bootstrap 方向频率及效应百分位。明确 source A/B、外部入级研究、外部不入级研究、边界及开发来源复用的角色。保留不同向和缺测。蛋白沿用来源表，区分差异记录支持、只测得同号、测得异号和缺测，不编造缺失 P 值或蛋白重复稳健性。
5. 选择变量与共同支持域。按原始 source-A 绝对效应和 source-B 同向确认检查每一 assay universe 的选择对齐候选背景。记录原始源覆盖、筛后背景数、范围重叠、最多可达到的非签名平均源效应及标准化差异下界。若源选择使经验支持域不重叠，明确全签名的选择调整富集不可识别，不外推或输出貌似有效的调整 P 值。GSE267238 匹配保留为替代背景敏感性，不称为控制了原始选择。
6. 多重性。完整保留旧主图的三个 assay-specific matched-null 检验作为一个探索性层级家族，统一补报 BH 调整，不择优删除。共享背景蛋白、磷酸化和 GO 为其他已记录敏感性，保留结果与定义，不将其升级为独立验证。没有选择调整的确认性富集主张。
7. 资源语义。新增 `display_order` 和 `occupancy_and_perturbation_supported` 明确字段，保留兼容性别名并注明旧字段含义。完整公开排序规则，修正 EXP1 元数据，补齐跨层来源清单。所有更改须通过回归测试证明未改变成员和等级。

## 正文放置

对称对照改变核心增量解释，应出现在摘要与结果，不藏于补充材料。选择混杂与不确定性边界保留于正文。完整 bootstrap 表、匹配支持域和候选逐情境结果放入 SI。优先替换旧解释，避免不断追加防御性段落。

AI 披露必须同步新增补分析代码、统计分析和图表修订的实际用途，不再仅描述评审诊断或英文润色。作者须复核本轮新结果。
